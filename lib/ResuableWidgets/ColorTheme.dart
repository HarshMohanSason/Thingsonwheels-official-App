
import 'package:flutter/material.dart';

class ColorTheme {
  static const LinearGradient gradient = LinearGradient(
    colors: [Colors.orange, Colors.deepOrange], // Adjusted for simplicity
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
}

